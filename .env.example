# Medicare EOB Parser - Environment Variables
# Copy this file to .env and update with your values

# Environment
NODE_ENV=development

# Server Configuration
PORT=3000

# Security - JWT (REQUIRED - Change these in production!)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRE=1h

# Security - Sessions (REQUIRED - Change these in production!)
SESSION_SECRET=your-super-secret-session-key-change-this-in-production
SESSION_TIMEOUT=900000  # 15 minutes in milliseconds

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com

# Logging
LOG_LEVEL=audit  # Options: error, warn, info, audit, debug

# Rate Limiting
RATE_LIMIT_WINDOW=900000  # 15 minutes in milliseconds
RATE_LIMIT_MAX=100  # Max requests per window
AUTH_RATE_LIMIT_MAX=5  # Max auth attempts per window

# File Upload
MAX_FILE_SIZE=10485760  # 10MB in bytes

# Password Policy
PASSWORD_MIN_LENGTH=8
PASSWORD_REQUIRE_UPPERCASE=true
PASSWORD_REQUIRE_LOWERCASE=true
PASSWORD_REQUIRE_NUMBER=true
PASSWORD_REQUIRE_SPECIAL=true

# Audit Log Retention
AUDIT_LOG_MAX_FILES=100
AUDIT_LOG_MAX_SIZE=10485760  # 10MB per file

# Production Security (Railway.app)
# These are automatically set by Railway in production
# RAILWAY_ENVIRONMENT=production
# RAILWAY_PROJECT_ID=your-project-id
# RAILWAY_SERVICE_ID=your-service-id

# Database (Future Enhancement)
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=medicare_eob
# DB_USER=dbuser
# DB_PASSWORD=dbpassword

# Email Notifications (Future Enhancement)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# ADMIN_EMAIL=admin@yourorg.com

# External Services (Future Enhancement)
# VIRUS_SCAN_API_KEY=your-virustotal-api-key
# SIEM_ENDPOINT=https://your-siem-endpoint.com
# BACKUP_S3_BUCKET=your-backup-bucket